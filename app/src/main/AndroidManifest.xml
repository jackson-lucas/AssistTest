<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="app.testbuilder">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Register"
            android:label="@string/title_activity_jsonpost">
        </activity>

        <activity
            android:name=".CadastroUI"
            android:label="@string/title_activity_cadastrar">
        </activity>

        <activity
            android:name=".ASSISTPergunta3"
            android:label="@string/title_activities_assist" >
        </activity>

        <activity
            android:name=".ASSISTPergunta2"
            android:label="@string/title_activities_assist" >
        </activity>

        <activity
            android:name=".ASSISTPergunta1"
            android:label="@string/title_activities_assist" >
        </activity>

        <activity
            android:name=".Resultado"
            android:label="@string/title_activity_resultados" >
        </activity>

    </application>

</manifest>
